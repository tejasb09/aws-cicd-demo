// Fallback greeting used when the app secret has no `greeting` field.
export const DEFAULT_GREETING = 'Hello, world!';

// Primary key of the sample item the hello-world Lambda reads from DynamoDB.
export const SAMPLE_ITEM_ID = 'hello';
